import UserNavigationLinks from 'components/UserNavigationLinks';

const ProfileNavigation = () => {
  return (
    <div>
      <UserNavigationLinks isInMenuProvider={false} />
    </div>
  );
};

export default ProfileNavigation;
